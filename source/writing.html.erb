---
date: 2020-09-01 00:00 UTC
layout: page
---

<h1>Writing</h1>

<h2 id="opinions" class="u-margin-top--double">Opinions</h2>

<ul>
  <% pages_by_category('opinions').sort_by(&:date).reverse.each do |article| %>
    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>

<h2 id="film-reviews" class="u-margin-top--double">Film Reviews</h2>

<ul>
  <% pages_by_category('film-reviews').sort_by(&:date).reverse.each do |article| %>
    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>

<h2 id="theatre-reviews" class="u-margin-top--double">Theatre Reviews</h2>

<ul>
  <% pages_by_category('theatre-reviews').sort_by(&:date).reverse.each do |article| %>
    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>
